% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmr_plan.R
\name{pilot_plan}
\alias{pilot_plan}
\alias{cmr_plan}
\title{Pilot/main-wave planning summary}
\usage{
pilot_plan(
  n,
  sigma1,
  sigma0,
  alpha = 0.05,
  method = c("bounded", "bernoulli"),
  input = c("sd", "variance"),
  accounting = c("design_only", "pooled"),
  desired_pilot = NULL,
  strict_upper = TRUE
)

cmr_plan(
  n,
  sigma1,
  sigma0,
  alpha = 0.05,
  method = c("bounded", "bernoulli"),
  input = c("sd", "variance"),
  accounting = c("design_only", "pooled"),
  desired_pilot = NULL,
  strict_upper = TRUE
)
}
\arguments{
\item{n}{Total experimental sample size across pilot and main wave.}

\item{sigma1}{Treatment-arm standard deviation, or variance when
\code{input = "variance"}.}

\item{sigma0}{Control-arm standard deviation, or variance when
\code{input = "variance"}.}

\item{alpha}{Target error level.}

\item{method}{Planning method, either \code{"bounded"} or \code{"bernoulli"}.}

\item{input}{Whether \code{sigma1} and \code{sigma0} are standard deviations (\code{"sd"})
or variances (\code{"variance"}).}

\item{accounting}{\code{"design_only"} applies the break-even pilot-share cap;
\code{"pooled"} ignores that cap.}

\item{desired_pilot}{Optional user-proposed pilot size to classify.}

\item{strict_upper}{If \code{TRUE}, feasible pilot sizes must be strictly below
the design-only break-even cap.}
}
\value{
A list of class \code{cmr_pilot_plan} with \code{band}, \code{suggested_pilot},
\code{default_two_thirds_power}, \code{desired_pilot}, \code{desired_status}, a text
recommendation, and a caveat that the screen is necessary rather than
sufficient.
}
\description{
Summarize the pilot-size viability band and a default pilot-size suggestion.
}
\examples{
cmr_plan(
  n = 3000,
  sigma1 = 0.18,
  sigma0 = 0.28,
  desired_pilot = 120
)

}
\seealso{
Other pilot planning: 
\code{\link{activation_threshold_bounded}()},
\code{\link{break_even_pilot_share}()},
\code{\link{pilot_viability_band}()}
}
\concept{pilot planning}
