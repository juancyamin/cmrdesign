% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_variance.R
\name{variance_objective}
\alias{variance_objective}
\alias{oracle_variance}
\alias{assign_neyman}
\alias{regret}
\title{Two-arm variance objectives and Neyman allocation}
\usage{
variance_objective(pi, v1, v0)

oracle_variance(v1, v0)

assign_neyman(v1, v0)

regret(pi, v1, v0)
}
\arguments{
\item{pi}{Treatment assignment share. Values must lie in \verb{[0, 1]}.}

\item{v1}{Treatment-arm outcome variance. For bounded or Bernoulli outcomes,
this must lie in \verb{[0, 1/4]}.}

\item{v0}{Control-arm outcome variance. For bounded or Bernoulli outcomes,
this must lie in \verb{[0, 1/4]}.}
}
\value{
Numeric vector after ordinary R recycling of \code{pi}, \code{v1}, and \code{v0}.
\code{variance_objective()} returns \code{v1 / pi + v0 / (1 - pi)},
\code{oracle_variance()} returns the Neyman-oracle value
\code{(sqrt(v1) + sqrt(v0))^2}, \code{assign_neyman()} returns the treatment share,
and \code{regret()} returns excess variance relative to the oracle.
}
\description{
Helper functions for the two-arm variance objective, oracle variance,
Neyman allocation, and regret. These are useful for checking CMR certificates
and comparing CMR against oracle or feasible-Neyman benchmarks.
}
\examples{
v1 <- 0.12
v0 <- 0.04
pi <- assign_neyman(v1, v0)
variance_objective(pi, v1, v0)
oracle_variance(v1, v0)
regret(0.5, v1, v0)

}
\seealso{
Other assignment helpers: 
\code{\link{assign_balance}()},
\code{\link{multiarm_variance_objective}()},
\code{\link{stratified_variance_objective}()}
}
\concept{assignment helpers}
