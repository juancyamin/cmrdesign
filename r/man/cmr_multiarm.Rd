% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmr_multiarm.R
\name{cmr_multiarm}
\alias{cmr_multiarm}
\title{Shared-control multi-arm CMR assignment}
\usage{
cmr_multiarm(
  y,
  arm,
  alpha = 0.05,
  method = c("auto", "bounded", "bernoulli", "maurer_pontil", "mp", "bernoulli_exact",
    "martinez_taboada_ramdas", "mtr"),
  beta = NULL,
  control_arm = 0,
  normalize = FALSE,
  lower = NULL,
  upper = NULL,
  na.rm = TRUE,
  tol = 1e-11,
  solver_control = list(),
  max_vertices = 65536L
)
}
\arguments{
\item{y}{Pilot outcomes.}

\item{arm}{Pilot arm labels. The control arm is identified by \code{control_arm}
and internally standardized to \code{"0"}.}

\item{alpha}{Target joint error level.}

\item{method}{Confidence-set method. \code{"auto"} chooses exact Bernoulli bounds
for 0/1 outcomes and bounded Maurer-Pontil bounds otherwise.}

\item{beta}{Optional endpoint error allocation. If \code{NULL}, Bonferroni error
is split across all lower and upper arm endpoints.}

\item{control_arm}{Label identifying the control arm in \code{arm}.}

\item{normalize}{If \code{TRUE}, normalize bounded outcomes to \verb{[0, 1]} before
computing variances.}

\item{lower, upper}{Optional lower and upper outcome bounds used when
\code{normalize = TRUE}.}

\item{na.rm}{If \code{TRUE}, drop rows with missing \code{y} or \code{arm}.}

\item{tol}{Numerical tolerance for exact Bernoulli bound inversion.}

\item{solver_control}{Optional list of solver controls for the general
vertex epigraph solver.}

\item{max_vertices}{Maximum number of hyperrectangle vertices to enumerate.}
}
\value{
A list of class \code{cmr_multiarm} with named assignment shares \code{pi} over all
arms, CMR certificate \code{U_CMR}, confidence set, pilot summaries, endpoint
error allocation, and diagnostics.
}
\description{
Estimate arm-specific variance confidence intervals from pilot data and
return the shared-control multi-arm CMR assignment.
}
\examples{
set.seed(5)
arm <- rep(c(0, 1, 2), each = 20)
y <- c(rbeta(20, 4, 4), rbeta(20, 2, 6), rbeta(20, 5, 3))
cmr_multiarm(y, arm, method = "bounded")

}
\seealso{
Other CMR rules: 
\code{\link{binary_rectangle_corners}()},
\code{\link{cmr_multiarm_from_rectangle}()},
\code{\link{cmr_multiple_outcomes}()},
\code{\link{cmr_proxy}()},
\code{\link{cmr_stratified}()},
\code{\link{cmr_stratified_from_rectangle}()},
\code{\link{cmr_two_arm}()},
\code{\link{cmr_unbounded}()},
\code{\link{cmr_unbounded_from_rectangle}()},
\code{\link{print.cmr_two_arm}()}
}
\concept{CMR rules}
