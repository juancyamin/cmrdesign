% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rectangles_proxy.R
\name{rectangle_proxy}
\alias{rectangle_proxy}
\alias{rectangle_delayed_outcome}
\title{Proxy or delayed-outcome confidence rectangle}
\usage{
rectangle_proxy(
  proxy_y,
  d,
  zeta,
  alpha = 0.05,
  method = c("auto", "bounded", "bernoulli", "maurer_pontil", "mp", "bernoulli_exact",
    "martinez_taboada_ramdas", "mtr"),
  beta = NULL,
  correction = c("bonferroni", "sidak_arms"),
  normalize = FALSE,
  lower = NULL,
  upper = NULL,
  na.rm = TRUE,
  tol = 1e-11
)

rectangle_delayed_outcome(
  proxy_y,
  d,
  zeta,
  alpha = 0.05,
  method = c("auto", "bounded", "bernoulli", "maurer_pontil", "mp", "bernoulli_exact",
    "martinez_taboada_ramdas", "mtr"),
  beta = NULL,
  correction = c("bonferroni", "sidak_arms"),
  normalize = FALSE,
  lower = NULL,
  upper = NULL,
  na.rm = TRUE,
  tol = 1e-11
)
}
\arguments{
\item{proxy_y}{Pilot proxy or delayed primary outcomes.}

\item{d}{Pilot treatment indicator; treatment is \code{1} and control is \code{0}.}

\item{zeta}{Nonnegative standard-deviation bridge radius. Provide a scalar
shared across arms or a treatment/control pair.}

\item{alpha}{Target joint error level.}

\item{method}{Confidence-set method. \code{"auto"} chooses exact Bernoulli bounds
for 0/1 outcomes and bounded Maurer–Pontil bounds otherwise.}

\item{beta}{Optional endpoint error allocation. If \code{NULL}, error is split
according to \code{correction}.}

\item{correction}{Endpoint error correction, either \code{"bonferroni"} or
\code{"sidak_arms"}.}

\item{normalize}{If \code{TRUE}, normalize bounded proxy outcomes to \verb{[0, 1]}
before computing variances.}

\item{lower, upper}{Optional lower and upper outcome bounds used when
\code{normalize = TRUE}.}

\item{na.rm}{If \code{TRUE}, drop rows with missing \code{proxy_y} or \code{d}.}

\item{tol}{Numerical tolerance for exact Bernoulli bound inversion.}
}
\value{
A list of class \code{cmr_proxy_rectangle} and \code{cmr_binary_rectangle} with the
widened primary-outcome rectangle, the underlying proxy confidence set,
\code{zeta}, bridge metadata, endpoint error allocation, pilot summaries, and
method metadata.
}
\description{
Construct a primary-outcome variance rectangle by estimating a proxy-outcome
rectangle and widening each arm's standard-deviation interval by \code{zeta}.
}
\examples{
set.seed(11)
d <- rep(c(1, 0), each = 30)
proxy_y <- c(rbeta(30, 2, 6), rbeta(30, 4, 4))
rectangle_proxy(proxy_y, d, zeta = 0.05)

}
\seealso{
Other rectangle helpers: 
\code{\link{binary_rectangle_corners}()},
\code{\link{cmr_multiarm_from_rectangle}()},
\code{\link{cmr_stratified_from_rectangle}()},
\code{\link{cmr_unbounded_from_rectangle}()},
\code{\link{folded_binomial_pmf}()},
\code{\link{multiarm_variance_objective}()},
\code{\link{rectangle_bernoulli_binary}()},
\code{\link{rectangle_bounded_two_arm}()},
\code{\link{rectangle_multiarm}()},
\code{\link{rectangle_multiple_outcomes}()},
\code{\link{rectangle_stratified}()},
\code{\link{rectangle_unbounded}()},
\code{\link{stratified_variance_objective}()},
\code{\link{variance_bounds_bernoulli_exact}()},
\code{\link{variance_bounds_maurer_pontil}()},
\code{\link{variance_bounds_unbounded_mom}()}
}
\concept{rectangle helpers}
