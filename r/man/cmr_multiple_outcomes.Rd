% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmr_multiple_outcomes.R
\name{cmr_multiple_outcomes}
\alias{cmr_multiple_outcomes}
\title{Multiple-outcome CMR assignment}
\usage{
cmr_multiple_outcomes(
  y,
  d,
  weights = NULL,
  estimand = c("coprimary", "index"),
  alpha = 0.05,
  method = c("auto", "bounded", "bernoulli", "maurer_pontil", "mp", "bernoulli_exact",
    "martinez_taboada_ramdas", "mtr"),
  beta = NULL,
  na.rm = TRUE,
  tol = 1e-11
)
}
\arguments{
\item{y}{Pilot outcomes as a numeric matrix, data frame, or vector. Rows are
units and columns are outcomes.}

\item{d}{Pilot treatment indicator; treatment is \code{1} and control is \code{0}.}

\item{weights}{Optional nonnegative outcome weights. If \code{NULL}, equal
weights are used.}

\item{estimand}{Either \code{"coprimary"} or \code{"index"}.}

\item{alpha}{Target joint error level.}

\item{method}{Confidence-set method. \code{"auto"} chooses exact Bernoulli bounds
for 0/1 outcomes and bounded Maurer-Pontil bounds otherwise.}

\item{beta}{Optional scalar endpoint error allocation.}

\item{na.rm}{If \code{TRUE}, drop rows with missing \code{y} or \code{d}.}

\item{tol}{Numerical tolerance for exact Bernoulli bound inversion.}
}
\value{
A list of class \code{cmr_multiple_outcomes} and \code{cmr_two_arm} with treatment
share \code{pi}, CMR certificate \code{U_CMR}, effective confidence rectangle, pilot
summaries, outcome weights, estimand metadata, endpoint error allocation, and
diagnostics.
}
\description{
Estimate an effective two-arm variance rectangle for multiple outcomes and
return the CMR treatment share.
}
\examples{
set.seed(10)
d <- rep(c(1, 0), each = 20)
y <- cbind(
  y1 = c(rbeta(20, 2, 6), rbeta(20, 4, 4)),
  y2 = c(rbeta(20, 5, 3), rbeta(20, 3, 5))
)
cmr_multiple_outcomes(y, d, weights = c(0.6, 0.4))

}
\seealso{
Other CMR rules: 
\code{\link{binary_rectangle_corners}()},
\code{\link{cmr_multiarm}()},
\code{\link{cmr_multiarm_from_rectangle}()},
\code{\link{cmr_proxy}()},
\code{\link{cmr_stratified}()},
\code{\link{cmr_stratified_from_rectangle}()},
\code{\link{cmr_two_arm}()},
\code{\link{cmr_unbounded}()},
\code{\link{cmr_unbounded_from_rectangle}()},
\code{\link{print.cmr_two_arm}()}
}
\concept{CMR rules}
