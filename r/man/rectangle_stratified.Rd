% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rectangles_stratified.R
\name{rectangle_stratified}
\alias{rectangle_stratified}
\title{Stratified confidence rectangle}
\usage{
rectangle_stratified(
  y,
  d,
  strata,
  strata_share,
  alpha = 0.05,
  method = c("auto", "bounded", "bernoulli", "maurer_pontil", "mp", "bernoulli_exact",
    "martinez_taboada_ramdas", "mtr"),
  beta = NULL,
  normalize = FALSE,
  lower = NULL,
  upper = NULL,
  na.rm = TRUE,
  tol = 1e-11
)
}
\arguments{
\item{y}{Pilot outcomes.}

\item{d}{Pilot treatment indicator; treatment is \code{1} and control is \code{0}.}

\item{strata}{Pilot stratum labels.}

\item{strata_share}{Named stratum population shares that sum to one.}

\item{alpha}{Target joint error level.}

\item{method}{Confidence-set method. \code{"auto"} chooses exact Bernoulli bounds
for 0/1 outcomes and bounded Maurer-Pontil bounds otherwise.}

\item{beta}{Optional endpoint error allocation. If \code{NULL}, Bonferroni error
is split across all lower and upper treatment/control by stratum endpoints.}

\item{normalize}{If \code{TRUE}, normalize bounded outcomes to \verb{[0, 1]} before
computing variances.}

\item{lower, upper}{Optional lower and upper outcome bounds used when
\code{normalize = TRUE}.}

\item{na.rm}{If \code{TRUE}, drop rows with missing \code{y}, \code{d}, or \code{strata}.}

\item{tol}{Numerical tolerance for exact Bernoulli bound inversion.}
}
\value{
A list of class \code{cmr_stratified_rectangle} with lower and upper rectangle
matrices, checked rectangle details, stratum shares, cell-level one-arm
bound results, endpoint error allocation, sample sizes, pilot variance
estimates, normalization details, and method metadata.
}
\description{
Construct cell-specific variance confidence intervals for a stratified
two-arm design.
}
\examples{
set.seed(8)
strata <- rep(c("A", "B"), each = 24)
d <- rep(rep(c(1, 0), each = 12), 2)
y <- c(rbeta(12, 2, 6), rbeta(12, 4, 4),
       rbeta(12, 5, 3), rbeta(12, 3, 5))
rectangle_stratified(y, d, strata, strata_share = c(A = 0.45, B = 0.55))

}
\seealso{
Other rectangle helpers: 
\code{\link{binary_rectangle_corners}()},
\code{\link{cmr_multiarm_from_rectangle}()},
\code{\link{cmr_stratified_from_rectangle}()},
\code{\link{cmr_unbounded_from_rectangle}()},
\code{\link{folded_binomial_pmf}()},
\code{\link{multiarm_variance_objective}()},
\code{\link{rectangle_bernoulli_binary}()},
\code{\link{rectangle_bounded_two_arm}()},
\code{\link{rectangle_multiarm}()},
\code{\link{rectangle_multiple_outcomes}()},
\code{\link{rectangle_proxy}()},
\code{\link{rectangle_unbounded}()},
\code{\link{stratified_variance_objective}()},
\code{\link{variance_bounds_bernoulli_exact}()},
\code{\link{variance_bounds_maurer_pontil}()},
\code{\link{variance_bounds_unbounded_mom}()}
}
\concept{rectangle helpers}
