% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmr_two_arm.R
\name{cmr_two_arm}
\alias{cmr_two_arm}
\alias{cmr_binary}
\title{Two-arm Conditional Minimax Regret assignment}
\usage{
cmr_two_arm(
  y,
  d,
  alpha = 0.05,
  method = c("auto", "bounded", "bernoulli", "maurer_pontil", "mp", "bernoulli_exact",
    "martinez_taboada_ramdas", "mtr", "unbounded", "unbounded_mom", "median_of_means",
    "mom"),
  beta = NULL,
  correction = c("bonferroni", "sidak_arms"),
  normalize = FALSE,
  lower = NULL,
  upper = NULL,
  psi = NULL,
  na.rm = TRUE,
  tol = 1e-11
)

cmr_binary(
  y,
  d,
  alpha = 0.05,
  method = c("auto", "bounded", "bernoulli", "maurer_pontil", "mp", "bernoulli_exact",
    "martinez_taboada_ramdas", "mtr", "unbounded", "unbounded_mom", "median_of_means",
    "mom"),
  beta = NULL,
  correction = c("bonferroni", "sidak_arms"),
  normalize = FALSE,
  lower = NULL,
  upper = NULL,
  psi = NULL,
  na.rm = TRUE,
  tol = 1e-11
)
}
\arguments{
\item{y}{Pilot outcomes. For bounded and Bernoulli methods, outcomes must be
in \verb{[0, 1]} unless \code{normalize = TRUE}. For unbounded methods, outcomes are
raw numeric values and \code{psi} is required.}

\item{d}{Pilot treatment indicator; treatment is \code{1} and control is \code{0}.}

\item{alpha}{Target joint error level for the variance confidence set.}

\item{method}{Confidence-set method. \code{"auto"} uses exact Bernoulli bounds for
0/1 outcomes and bounded Maurer–Pontil bounds otherwise. \code{"bounded"},
\code{"maurer_pontil"}, and \code{"mp"} are synonyms. \code{"bernoulli"} and
\code{"bernoulli_exact"} use folded-binomial exact bounds. \code{"mtr"} and
\code{"martinez_taboada_ramdas"} use the empirical-Bernstein MTR bounds.
\code{"unbounded"}, \code{"unbounded_mom"}, \code{"median_of_means"}, and \code{"mom"} dispatch
to the unbounded-outcome median-of-means extension.}

\item{beta}{Optional endpoint error allocation. If \code{NULL}, error is split
across lower and upper endpoints using \code{correction}.}

\item{correction}{Endpoint error correction, either \code{"bonferroni"} or
\code{"sidak_arms"} for two-arm bounded/Bernoulli/proxy workflows.}

\item{normalize}{If \code{TRUE}, normalize bounded outcomes to \verb{[0, 1]} before
computing the rectangle.}

\item{lower, upper}{Optional lower and upper outcome bounds used when
\code{normalize = TRUE}.}

\item{psi}{Bounded-kurtosis parameter for unbounded-outcome methods. Provide
a scalar or a treatment/control pair.}

\item{na.rm}{If \code{TRUE}, drop rows with missing \code{y} or \code{d}.}

\item{tol}{Numerical tolerance for exact Bernoulli bound inversion.}
}
\value{
A list of class \code{cmr_two_arm} with treatment share \code{pi}, regret certificate
\code{U_CMR}, confidence rectangle, pilot summaries, endpoint error allocation,
and diagnostics. The object has compact \code{print()} and \code{summary()} methods.
}
\description{
Estimate a finite-sample variance confidence rectangle from pilot data and
return the two-arm Conditional Minimax Regret (CMR) assignment.
}
\examples{
set.seed(1)
d <- rep(c(1, 0), each = 40)
y <- c(rbeta(40, 2, 6), rbeta(40, 4, 4))

fit <- cmr_two_arm(y, d, alpha = 0.05, method = "bounded")
fit
summary(fit)

}
\seealso{
Other CMR rules: 
\code{\link{binary_rectangle_corners}()},
\code{\link{cmr_multiarm}()},
\code{\link{cmr_multiarm_from_rectangle}()},
\code{\link{cmr_multiple_outcomes}()},
\code{\link{cmr_proxy}()},
\code{\link{cmr_stratified}()},
\code{\link{cmr_stratified_from_rectangle}()},
\code{\link{cmr_unbounded}()},
\code{\link{cmr_unbounded_from_rectangle}()},
\code{\link{print.cmr_two_arm}()}
}
\concept{CMR rules}
