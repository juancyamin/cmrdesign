% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmr_unbounded.R
\name{cmr_unbounded}
\alias{cmr_unbounded}
\title{Unbounded-outcome CMR assignment}
\usage{
cmr_unbounded(y, d, psi = NULL, alpha = 0.05, na.rm = TRUE)
}
\arguments{
\item{y}{Pilot outcomes.}

\item{d}{Pilot treatment indicator; treatment is \code{1} and control is \code{0}.}

\item{psi}{Bounded-kurtosis parameter, either a scalar shared across arms or
a treatment/control pair.}

\item{alpha}{Target joint error level. Each arm uses
\code{\link[=variance_bounds_unbounded_mom]{variance_bounds_unbounded_mom()}} with this same \code{alpha}, whose
median-of-means block count gives one-arm error at most \code{alpha / 2}; the
union bound over treatment and control yields the reported joint error
bound \code{alpha}.}

\item{na.rm}{If \code{TRUE}, drop rows with missing \code{y} or \code{d}.}
}
\value{
A list of class \code{cmr_unbounded} and \code{cmr_two_arm}. If both one-arm bounds are
active, the object contains \code{pi}, finite \code{U_CMR}, rectangle, pilot summaries,
and diagnostics. If the pilot is inactive, the function returns balance
(\code{pi = 0.5}) with \code{U_CMR = Inf} and diagnostics explaining the fallback.
}
\description{
Estimate a median-of-means variance rectangle from raw pilot outcomes and
return the unbounded-outcome CMR assignment.
}
\examples{
set.seed(4)
d <- rep(c(1, 0), each = 220)
y <- c(rnorm(220, sd = 1.3), rnorm(220, sd = 0.8))
cmr_unbounded(y, d, psi = 3)

}
\seealso{
Other CMR rules: 
\code{\link{binary_rectangle_corners}()},
\code{\link{cmr_multiarm}()},
\code{\link{cmr_multiarm_from_rectangle}()},
\code{\link{cmr_multiple_outcomes}()},
\code{\link{cmr_proxy}()},
\code{\link{cmr_stratified}()},
\code{\link{cmr_stratified_from_rectangle}()},
\code{\link{cmr_two_arm}()},
\code{\link{cmr_unbounded_from_rectangle}()},
\code{\link{print.cmr_two_arm}()}
}
\concept{CMR rules}
