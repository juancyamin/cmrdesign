% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline_rules.R
\name{assign_balance}
\alias{assign_balance}
\alias{assign_multiarm_balance}
\alias{assign_stratified_balance}
\alias{assign_feasible_neyman}
\alias{assign_trimmed_neyman}
\alias{assign_additive_regularized_neyman}
\alias{assign_exponential_regularized_neyman}
\title{Baseline and comparator assignment rules}
\usage{
assign_balance(n = 1L)

assign_multiarm_balance(arms)

assign_stratified_balance(strata_share)

assign_feasible_neyman(vhat1, vhat0)

assign_trimmed_neyman(vhat1, vhat0, trim = 0.1)

assign_additive_regularized_neyman(vhat1, vhat0, nu)

assign_exponential_regularized_neyman(
  vhat1,
  vhat0,
  tau,
  zero_guard = c("any", "both", "none")
)
}
\arguments{
\item{n}{Number of assignment shares to return for \code{assign_balance()}.}

\item{arms}{Either the number of treatment arms, excluding control, or a
vector of arm labels that includes control arm \code{"0"}.}

\item{strata_share}{Named stratum population shares that sum to one.}

\item{vhat1}{Estimated treatment-arm variance or vector of estimates.}

\item{vhat0}{Estimated control-arm variance or vector of estimates.}

\item{trim}{Lower and upper trimming amount for \code{assign_trimmed_neyman()}.
The returned share is clipped to \verb{[trim, 1 - trim]}.}

\item{nu}{Nonnegative additive regularization strength.}

\item{tau}{Nonnegative exponent for exponential regularization.}

\item{zero_guard}{How zero variance estimates are guarded in
\code{assign_exponential_regularized_neyman()}: \code{"any"} returns balance if
either arm variance is zero, \code{"both"} only if both are zero, and \code{"none"}
applies no extra guard.}
}
\value{
Numeric assignment shares. Two-arm functions return treatment shares.
\code{assign_multiarm_balance()} returns a named vector over all arms, including
control \code{"0"}. \code{assign_stratified_balance()} returns total assignment shares
for treatment and control cells named like \code{"1:A"} and \code{"0:A"}.
}
\description{
Standalone benchmark assignment rules for comparing CMR against balance,
feasible Neyman, and simple regularized Neyman variants.
}
\examples{
assign_balance(3)
assign_feasible_neyman(0.12, 0.04)
assign_trimmed_neyman(0.12, 0.04, trim = 0.10)
assign_multiarm_balance(2)
assign_stratified_balance(c(A = 0.4, B = 0.6))

}
\seealso{
Other assignment helpers: 
\code{\link{multiarm_variance_objective}()},
\code{\link{stratified_variance_objective}()},
\code{\link{variance_objective}()}
}
\concept{assignment helpers}
