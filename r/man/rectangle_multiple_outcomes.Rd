% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rectangles_multiple_outcomes.R
\name{rectangle_multiple_outcomes}
\alias{rectangle_multiple_outcomes}
\title{Multiple-outcome confidence rectangle}
\usage{
rectangle_multiple_outcomes(
  y,
  d,
  weights = NULL,
  estimand = c("coprimary", "index"),
  alpha = 0.05,
  method = c("auto", "bounded", "bernoulli", "maurer_pontil", "mp", "bernoulli_exact",
    "martinez_taboada_ramdas", "mtr"),
  beta = NULL,
  na.rm = TRUE,
  tol = 1e-11
)
}
\arguments{
\item{y}{Pilot outcomes as a numeric matrix, data frame, or vector. Rows are
units and columns are outcomes.}

\item{d}{Pilot treatment indicator; treatment is \code{1} and control is \code{0}.}

\item{weights}{Optional nonnegative outcome weights. If \code{NULL}, equal
weights are used.}

\item{estimand}{Either \code{"coprimary"} or \code{"index"}.}

\item{alpha}{Target joint error level.}

\item{method}{Confidence-set method. \code{"auto"} chooses exact Bernoulli bounds
for 0/1 outcomes and bounded Maurer-Pontil bounds otherwise.}

\item{beta}{Optional scalar endpoint error allocation.}

\item{na.rm}{If \code{TRUE}, drop rows with missing \code{y} or \code{d}.}

\item{tol}{Numerical tolerance for exact Bernoulli bound inversion.}
}
\value{
A list of class \code{cmr_multiple_outcomes_rectangle}. For \code{estimand = "index"},
this wraps the ordinary two-arm rectangle for the weighted index. For
\code{estimand = "coprimary"}, it contains the effective two-arm \code{rectangle},
weights, outcome-specific bounds, pilot summaries, endpoint error allocation,
and method metadata.
}
\description{
Construct an effective two-arm variance rectangle for multiple outcomes.
For \code{estimand = "index"}, outcomes are first combined using \code{weights}. For
\code{estimand = "coprimary"}, one rectangle is built per outcome and combined
conservatively using the outcome weights.
}
\examples{
set.seed(9)
d <- rep(c(1, 0), each = 20)
y <- cbind(
  y1 = c(rbeta(20, 2, 6), rbeta(20, 4, 4)),
  y2 = c(rbeta(20, 5, 3), rbeta(20, 3, 5))
)
rectangle_multiple_outcomes(y, d, weights = c(0.6, 0.4))

}
\seealso{
Other rectangle helpers: 
\code{\link{binary_rectangle_corners}()},
\code{\link{cmr_multiarm_from_rectangle}()},
\code{\link{cmr_stratified_from_rectangle}()},
\code{\link{cmr_unbounded_from_rectangle}()},
\code{\link{folded_binomial_pmf}()},
\code{\link{multiarm_variance_objective}()},
\code{\link{rectangle_bernoulli_binary}()},
\code{\link{rectangle_bounded_two_arm}()},
\code{\link{rectangle_multiarm}()},
\code{\link{rectangle_proxy}()},
\code{\link{rectangle_stratified}()},
\code{\link{rectangle_unbounded}()},
\code{\link{stratified_variance_objective}()},
\code{\link{variance_bounds_bernoulli_exact}()},
\code{\link{variance_bounds_maurer_pontil}()},
\code{\link{variance_bounds_unbounded_mom}()}
}
\concept{rectangle helpers}
