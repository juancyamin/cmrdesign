% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmr_stratified.R
\name{cmr_stratified}
\alias{cmr_stratified}
\title{Stratified CMR assignment}
\usage{
cmr_stratified(
  y,
  d,
  strata,
  strata_share,
  alpha = 0.05,
  method = c("auto", "bounded", "bernoulli", "maurer_pontil", "mp", "bernoulli_exact",
    "martinez_taboada_ramdas", "mtr"),
  beta = NULL,
  normalize = FALSE,
  lower = NULL,
  upper = NULL,
  na.rm = TRUE,
  tol = 1e-11,
  solver_control = list(),
  max_vertices = 65536L
)
}
\arguments{
\item{y}{Pilot outcomes.}

\item{d}{Pilot treatment indicator; treatment is \code{1} and control is \code{0}.}

\item{strata}{Pilot stratum labels.}

\item{strata_share}{Named stratum population shares that sum to one.}

\item{alpha}{Target joint error level.}

\item{method}{Confidence-set method. \code{"auto"} chooses exact Bernoulli bounds
for 0/1 outcomes and bounded Maurer-Pontil bounds otherwise.}

\item{beta}{Optional endpoint error allocation. If \code{NULL}, Bonferroni error
is split across all lower and upper treatment/control by stratum endpoints.}

\item{normalize}{If \code{TRUE}, normalize bounded outcomes to \verb{[0, 1]} before
computing variances.}

\item{lower, upper}{Optional lower and upper outcome bounds used when
\code{normalize = TRUE}.}

\item{na.rm}{If \code{TRUE}, drop rows with missing \code{y}, \code{d}, or \code{strata}.}

\item{tol}{Numerical tolerance for exact Bernoulli bound inversion.}

\item{solver_control}{Optional list of solver controls for the general
vertex epigraph solver.}

\item{max_vertices}{Maximum number of hyperrectangle vertices to enumerate.}
}
\value{
A list of class \code{cmr_stratified} with total cell assignment shares \code{pi},
matrix form \code{pi_matrix}, sampling and treatment margins, CMR certificate
\code{U_CMR}, confidence set, pilot summaries, endpoint error allocation, and
diagnostics.
}
\description{
Estimate cell-specific variance confidence intervals from pilot data and
return the stratified CMR assignment across treatment/control by stratum
cells.
}
\examples{
set.seed(7)
strata <- rep(c("A", "B"), each = 40)
d <- rep(rep(c(1, 0), each = 20), 2)
y <- c(rbeta(20, 2, 6), rbeta(20, 4, 4),
       rbeta(20, 5, 3), rbeta(20, 3, 5))
cmr_stratified(y, d, strata, strata_share = c(A = 0.45, B = 0.55))

}
\seealso{
Other CMR rules: 
\code{\link{binary_rectangle_corners}()},
\code{\link{cmr_multiarm}()},
\code{\link{cmr_multiarm_from_rectangle}()},
\code{\link{cmr_multiple_outcomes}()},
\code{\link{cmr_proxy}()},
\code{\link{cmr_stratified_from_rectangle}()},
\code{\link{cmr_two_arm}()},
\code{\link{cmr_unbounded}()},
\code{\link{cmr_unbounded_from_rectangle}()},
\code{\link{print.cmr_two_arm}()}
}
\concept{CMR rules}
