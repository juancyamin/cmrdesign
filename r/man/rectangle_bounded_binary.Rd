% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rectangles_bernoulli.R, R/rectangles_bounded.R
\name{rectangle_bounded_two_arm}
\alias{rectangle_bounded_two_arm}
\alias{rectangle_bounded_binary}
\title{Bounded two-arm confidence rectangle}
\usage{
rectangle_bounded_two_arm(
  y,
  d,
  alpha = 0.05,
  method = c("bounded", "maurer_pontil", "mp", "martinez_taboada_ramdas", "mtr"),
  beta = NULL,
  correction = c("bonferroni", "sidak_arms"),
  normalize = FALSE,
  lower = NULL,
  upper = NULL,
  na.rm = TRUE
)

rectangle_bounded_binary(
  y,
  d,
  alpha = 0.05,
  method = c("bounded", "maurer_pontil", "mp", "martinez_taboada_ramdas", "mtr"),
  beta = NULL,
  correction = c("bonferroni", "sidak_arms"),
  normalize = FALSE,
  lower = NULL,
  upper = NULL,
  na.rm = TRUE
)
}
\arguments{
\item{y}{Pilot outcomes.}

\item{d}{Pilot treatment indicator; treatment is \code{1} and control is \code{0}.}

\item{alpha}{Target joint error level.}

\item{method}{Bounded-outcome method. \code{"bounded"}, \code{"maurer_pontil"}, and
\code{"mp"} are synonyms; \code{"martinez_taboada_ramdas"} and \code{"mtr"} use MTR
bounds.}

\item{beta}{Optional endpoint error allocation. If \code{NULL}, error is split
according to \code{correction}.}

\item{correction}{Endpoint error correction, either \code{"bonferroni"} or
\code{"sidak_arms"}.}

\item{normalize}{If \code{TRUE}, normalize outcomes to \verb{[0, 1]} before computing
variances.}

\item{lower, upper}{Optional lower and upper outcome bounds used when
\code{normalize = TRUE}.}

\item{na.rm}{If \code{TRUE}, drop rows with missing \code{y} or \code{d}.}
}
\value{
A \code{cmr_binary_rectangle} list with \code{rectangle}, one-arm bound objects for
treatment and control, endpoint error allocation, sample sizes, pilot
variance estimates, normalization details, and method metadata.
}
\description{
Construct a two-arm variance confidence rectangle for bounded outcomes using
Maurer–Pontil or Martinez-Taboada–Ramdas one-arm bounds.
}
\examples{
d <- rep(c(1, 0), each = 5)
y <- c(0.20, 0.40, 0.30, 0.10, 0.60, 0.50, 0.30, 0.20, 0.40, 0.10)
rectangle_bounded_binary(y, d, method = "bounded")

}
\seealso{
Other rectangle helpers: 
\code{\link{binary_rectangle_corners}()},
\code{\link{cmr_multiarm_from_rectangle}()},
\code{\link{cmr_stratified_from_rectangle}()},
\code{\link{cmr_unbounded_from_rectangle}()},
\code{\link{folded_binomial_pmf}()},
\code{\link{multiarm_variance_objective}()},
\code{\link{rectangle_bernoulli_binary}()},
\code{\link{rectangle_multiarm}()},
\code{\link{rectangle_multiple_outcomes}()},
\code{\link{rectangle_proxy}()},
\code{\link{rectangle_stratified}()},
\code{\link{rectangle_unbounded}()},
\code{\link{stratified_variance_objective}()},
\code{\link{variance_bounds_bernoulli_exact}()},
\code{\link{variance_bounds_maurer_pontil}()},
\code{\link{variance_bounds_unbounded_mom}()}
}
\concept{rectangle helpers}
