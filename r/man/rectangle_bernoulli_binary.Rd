% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rectangles_bernoulli.R
\name{rectangle_bernoulli_binary}
\alias{rectangle_bernoulli_binary}
\alias{rectangle_binary}
\alias{rectangle_two_arm}
\alias{rectangle_bernoulli_two_arm}
\title{Two-arm confidence rectangles}
\usage{
rectangle_bernoulli_binary(
  y,
  d,
  alpha = 0.05,
  beta = NULL,
  correction = c("bonferroni", "sidak_arms"),
  na.rm = TRUE,
  tol = 1e-11
)

rectangle_binary(
  y,
  d,
  alpha = 0.05,
  method = c("auto", "bounded", "bernoulli", "maurer_pontil", "mp", "bernoulli_exact",
    "martinez_taboada_ramdas", "mtr", "unbounded", "unbounded_mom", "median_of_means",
    "mom"),
  beta = NULL,
  correction = c("bonferroni", "sidak_arms"),
  normalize = FALSE,
  lower = NULL,
  upper = NULL,
  psi = NULL,
  na.rm = TRUE,
  tol = 1e-11
)

rectangle_two_arm(
  y,
  d,
  alpha = 0.05,
  method = c("auto", "bounded", "bernoulli", "maurer_pontil", "mp", "bernoulli_exact",
    "martinez_taboada_ramdas", "mtr", "unbounded", "unbounded_mom", "median_of_means",
    "mom"),
  beta = NULL,
  correction = c("bonferroni", "sidak_arms"),
  normalize = FALSE,
  lower = NULL,
  upper = NULL,
  psi = NULL,
  na.rm = TRUE,
  tol = 1e-11
)

rectangle_bernoulli_two_arm(
  y,
  d,
  alpha = 0.05,
  beta = NULL,
  correction = c("bonferroni", "sidak_arms"),
  na.rm = TRUE,
  tol = 1e-11
)
}
\arguments{
\item{y}{Pilot outcomes.}

\item{d}{Pilot treatment indicator; treatment is \code{1} and control is \code{0}.}

\item{alpha}{Target joint error level.}

\item{beta}{Optional endpoint error allocation. If \code{NULL}, error is split
according to \code{correction}.}

\item{correction}{Endpoint error correction, either \code{"bonferroni"} or
\code{"sidak_arms"} for two-arm workflows.}

\item{na.rm}{If \code{TRUE}, drop rows with missing \code{y} or \code{d}.}

\item{tol}{Numerical tolerance for exact Bernoulli bound inversion.}

\item{method}{Confidence-set method. \code{"auto"} chooses exact Bernoulli bounds
for 0/1 outcomes and bounded Maurer-Pontil bounds otherwise.}

\item{normalize}{If \code{TRUE}, normalize bounded outcomes to \verb{[0, 1]} before
computing variances.}

\item{lower, upper}{Optional lower and upper outcome bounds used when
\code{normalize = TRUE}.}

\item{psi}{Bounded-kurtosis parameter used only when \code{method} is an
unbounded-outcome method.}
}
\value{
A rectangle object. Bounded and Bernoulli methods return a
\code{cmr_binary_rectangle}; unbounded methods return a
\code{cmr_unbounded_rectangle}. The object contains the numeric \code{rectangle},
one-arm bound details, endpoint error allocation, pilot sample sizes and
variance estimates, and method metadata.
}
\description{
Construct a two-arm variance confidence rectangle from pilot data. These are
expert helpers used by \code{cmr_two_arm()} and related extension functions.
}
\examples{
d <- rep(c(1, 0), each = 6)
y <- c(1, 0, 1, 1, 0, 1, 0, 0, 1, 0, 0, 1)
rectangle_two_arm(y, d, method = "bernoulli")
rectangle_binary(y, d, method = "auto")

}
\seealso{
Other rectangle helpers: 
\code{\link{binary_rectangle_corners}()},
\code{\link{cmr_multiarm_from_rectangle}()},
\code{\link{cmr_stratified_from_rectangle}()},
\code{\link{cmr_unbounded_from_rectangle}()},
\code{\link{folded_binomial_pmf}()},
\code{\link{multiarm_variance_objective}()},
\code{\link{rectangle_bounded_two_arm}()},
\code{\link{rectangle_multiarm}()},
\code{\link{rectangle_multiple_outcomes}()},
\code{\link{rectangle_proxy}()},
\code{\link{rectangle_stratified}()},
\code{\link{rectangle_unbounded}()},
\code{\link{stratified_variance_objective}()},
\code{\link{variance_bounds_bernoulli_exact}()},
\code{\link{variance_bounds_maurer_pontil}()},
\code{\link{variance_bounds_unbounded_mom}()}
}
\concept{rectangle helpers}
